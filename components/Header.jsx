import React from 'react'

const Header = (props) => {
  return (
    <div className="header d-flex align-items-center">
        <p6 className="title-header">{props.title}</p6>
    </div>
  )
}

export default Header